# version: '3.8'

name: crypto_bot

services:
  bot:
    build: .
    container_name: crypto_bot
    env_file: .env.docker
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - NATS_URL=${NATS_URL}
    volumes:
      - ./bot.py:/app/bot.py
      - ./database:/app/database
      - ./requirements.txt:/app/requirements.txt
      - ./alembic:/app/alembic
      - ./scripts:/app/scripts
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      nats:
        condition: service_started
    restart: always

  db:
    image: postgres:15
    env_file: .env.docker
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_MULTIPLE_DATABASES: crypto_bot_shard_0,crypto_bot_shard_1,crypto_bot_shard_2,crypto_bot_shard_3,crypto_bot_shard_4
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5433:5432"
    volumes:
      - ./init-shards.sh:/docker-entrypoint-initdb.d/init-shards.sh
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:6.2
    command: redis-server --appendonly yes --replica-read-only no
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  redis-replica-1:
    image: redis:6.2
    command: redis-server --appendonly yes --replica-read-only yes --replicaof redis 6379
    depends_on:
      - redis

  redis-replica-2:
    image: redis:6.2
    command: redis-server --appendonly yes --replica-read-only yes --replicaof redis 6379
    depends_on:
      - redis

  nats-1:
    image: nats:latest
    command: ["-js", "-sd", "/data", "--cluster", "nats://0.0.0.0:6222", "--routes=nats://nats-2:6222,nats://nats-3:6222"]
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - ./nats/data1:/data

  nats-2:
    image: nats:latest
    command: ["-js", "-sd", "/data", "--cluster", "nats://0.0.0.0:6222", "--routes=nats://nats-1:6222,nats://nats-3:6222"]
    volumes:
      - ./nats/data2:/data

  nats-3:
    image: nats:latest
    command: ["-js", "-sd", "/data", "--cluster", "nats://0.0.0.0:6222", "--routes=nats://nats-1:6222,nats://nats-2:6222"]
    volumes:
      - ./nats/data3:/data

volumes:
  pgdata:
  redis_data: